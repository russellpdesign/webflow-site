This directory contains individual javascript files that pertain to specific ui or scroll animations.

This directory was built prior to the vite build folder nested within it.

The webflow-vite-build is connected to git and triggers a deployment to cloudflare when new commits are pushed to git.

This directory is hosted on github and serves via static links.

